/// <reference types="vite/client" />

// Git hash of the running build, baked in by vite.config.ts — shown in the
// 🐞 debug panel so a stale checkout is obvious at a glance.
declare const __APP_VERSION__: string;
